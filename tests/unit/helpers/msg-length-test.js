import { msgLength } from '../../../helpers/msg-length';
import { module, test } from 'qunit';

module('Unit | Helper | msg length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = msgLength([42]);
  assert.ok(result);
});
